#include <cplib/general.h>
using namespace std;

int main() {
    int t; rd(t);
    while(t--) {
        int n, k; rd(n, k);
        if(k % 2 == 1) {
            cout << "NO" << endl;
        } else {
            
        }
    }
}